# Committed template for Backend/.env.prod — copy to .env.prod and fill PROD secrets.
# Prefer prod secrets on the production host only. Activate: bash scripts/switch-app-env.sh prod
APP_TYPE=prod
APP_ENV=prod

SHOPIFY_API_SECRET=
SHOPIFY_API_KEY=
INTERNAL_HANDOFF_SECRET=change-me-handoff-prod
TOKEN_ENCRYPTION_KEY=change-me-encryption-prod

APP_SECRET=change-me
APP_SECRET_KEY=change-me

SHOPIFY_SHOP_DOMAIN=your-prod-store.myshopify.com

CORS_ORIGINS=https://your-prod-frontend.example.com

HOST=0.0.0.0
PORT=8080

DATABASE_URL=postgresql+psycopg://user:password@127.0.0.1:5432/aone_image_enhancement_prod

POC_ENABLED=false
POC_MAX_PROMPT_STEPS=5
POC_MAX_IMAGE_SIZE_MB=10
POC_STORAGE_DIR=storage/poc-jobs
POC_JOB_TTL_HOURS=24

OPENAI_API_KEY=
OPENAI_IMAGE_MODEL=gpt-image-1
POC_DEV_SKIP_AUTH=false

AUTO_PROCESSING_ENABLED=true
PROCESSING_BATCH_SIZE=10
PROCESSING_BATCH_CONCURRENCY=2
PROCESSING_POLL_INTERVAL_SECONDS=5
PROCESSING_MAX_ATTEMPTS=3
PROCESSING_RETRY_DELAY_SECONDS=60
PROCESSING_ITEM_TIMEOUT_SECONDS=300
PROCESSING_STALE_LOCK_SECONDS=600
PROCESSING_OUTPUT_DIRECTORY=storage/processed

SHOPIFY_IMAGE_DOWNLOAD_TIMEOUT_SECONDS=60
SHOPIFY_IMAGE_MAX_DOWNLOAD_MB=30
SHOPIFY_API_VERSION=2026-07
SHOPIFY_DEV_ACCESS_TOKEN=

BATCH_INTERVAL_MINUTES_CAP=1440
DEFAULT_BATCH_INTERVAL_MINUTES=15
AUTO_BATCH_CLAIM_LIMIT=500
MANUAL_BATCH_PRODUCT_LIMIT=50
